// `ng build --dev` replaces `environment.ts` with `environment.dev.ts`.
export const environment = {
  production: false,
  // Local ENVIRONMENT URL
  API_URL: 'http://localhost:5000/api/modules',
  GOOGLE_WEB_CLIENT_ID: 'Your google Web client id', // Generate it from google developer account credentials
  FACEBOOK_APP_ID: 'Your Facebook APP ID' // Generate it from facebook developer account
};
